import { ReactElement } from "react";

/**
 * A component that renders a page for adding a product.
 *
 * The component renders a heading with the text "Add Product".
 *
 * @returns {ReactElement} A JSX element representing the add product page.
 */
const AddProduct = (): ReactElement => {
  return (
    <div>
      <h1>Add Product</h1>
    </div>
  );
};

export default AddProduct;
